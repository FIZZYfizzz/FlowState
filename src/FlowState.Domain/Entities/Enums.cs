namespace FlowState.Domain.Entities;

public enum TaskType
{
    Default = 0,
    DeepWork = 1,
    Admin = 2,
    Personal = 3
}

public enum FlexibilityType
{
    Fixed = 0,
    Flexible = 1,
    Optional = 2
}
